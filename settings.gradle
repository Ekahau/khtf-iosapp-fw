
pluginManagement {
    repositories {
        mavenCentral()
        google()
        jcenter()
        maven { url 'https://plugins.gradle.org/m2' }
        maven { url 'https://dl.bintray.com/kotlin/kotlin-eap' }
        maven { url 'https://dl.bintray.com/kotlin/kotlin-dev' }
    }
}

enableFeaturePreview('GRADLE_METADATA')

// name should contain only numbers and letters for dynamic framework to be resolved correctly
rootProject.name = 'KhtfIosAppFw'

